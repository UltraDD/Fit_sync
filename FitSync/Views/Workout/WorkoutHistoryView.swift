import SwiftUI

struct WorkoutHistoryView: View {
    var body: some View {
        List {
            Text("训练历史功能开发中").foregroundStyle(.secondary)
        }
        .navigationTitle("训练历史")
        .navigationBarTitleDisplayMode(.inline)
    }
}
